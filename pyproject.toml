[project]
name = "model-car"
version = "0.1.0"
dependencies = [
    "streamlit",
    "fastapi",
    "uvicorn",
    "joblib",
    "numpy",
    "pandas",
    "scikit-learn==1.6.1"
]

[project.scripts]
app = "app:app"
